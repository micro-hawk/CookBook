#include<bits/stdc++.h>
#define ll long long
using namespace std;
ll main() {
    ll t; cin >> t; while(t--) {
        ll len, count=0; string s; cin >> len >> s;
        for (ll i = 0; i < len; i++)
        {
            if(s[i]=='0') count++;
        } m 
        
    }
}